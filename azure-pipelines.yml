trigger:
- master


resources:
  repositories:
  - repository: azure
    type: github
    name: FasterMike/azure    
    endpoint: FasterMike

variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: 'a48be0ed-1f78-41d2-91bb-f56d2cd71017'
  imageRepository: 'fastermikeazure'
  containerRegistry: 'arcmiha.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
 # tag: '$(Build.BuildId)'
  imagePullSecret: 'arcmiha1619ec65-auth'
  tag: '$(Build.BuildId)'
  


  # Agent VM image name
  vmImageName: 'ubuntu-latest'




stages:

- template: build.yml@azure

- template: push.yml@azure

- template: deploy.yml@azure

