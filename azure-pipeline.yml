trigger:
- master


resources:
  repositories:
  - repository: azure
    type: github
    name: FasterMike/azure
    endpoint: FasterMike


variables:
  # Container registry service connection established during pipeline creation

  dockerRegistryServiceConnection: '123'
  imageRepository: 'fastimage'
  containerRegistry: 'fastermikeazure.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/Dockerfile'
  tag: '$(Build.BuildId)'
  imagePullSecret: 'arcmiha1726a60d-auth'
  vmImageName: 'ubuntu-latest'

#pool:
#  name: Default

stages:

- template: bp.yml@azure


#- template: push.yml@azure


- template: deploy.yml@azure
